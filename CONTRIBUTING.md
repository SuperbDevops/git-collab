# 참여 규칙

## 브랜치

```
<타입>/<작업내용>
```

| 접두 | 용도 |
| --- | --- |
| `feat/` | 기능 추가 |
| `fix/` | 결함 수정 |
| `docs/` | 문서 |
| `chore/` | 설정 · 정리 |

## 커밋 메시지

```
<타입>: <내용>
```

| 타입 | 용도 |
| --- | --- |
| `feat` | 기능 추가 |
| `fix` | 결함 수정 |
| `docs` | 문서 |
| `chore` | 설정 · 빌드 |
| `refactor` | 동작 변경 없는 개선 |
| `test` | 테스트 |
| `style` | 형식만 변경 |

무엇을 왜 변경했는지가 드러나야 함.

| 나쁜 예 | 좋은 예 |
| --- | --- |
| `수정` | `fix: 로그인 시 세션 만료 오류 수정` |
| `update file` | `chore: gitignore 에 빌드 결과 추가` |

## Pull Request

| 조건 | 내용 |
| --- | --- |
| 대상 | `main` |
| 이슈 연결 | `Closes #<번호>` 를 기재 |
| 검토자 | 최소 1명을 지정 |

## 코드 리뷰

### 확인 항목

```
  파일 내용이 요청과 일치하는가
  민감 정보가 포함되지 않았는가
  커밋 메시지가 규약을 준수하는가
  파일명이 브랜치 이름과 일치하는가
```

### 작성 원칙

| 항목 | 내용 |
| --- | --- |
| 대상 | 사람이 아니라 코드 |
| 근거 | 취향이 아니라 이유를 기재 |
| 범위 | 이 PR 의 목적에 한정 |
| 표현 | 질문 형태가 유효 |

## 금지 사항

| 항목 | 이유 |
| --- | --- |
| `main` 직접 전송 | 보호 설정으로 차단 |
| 강제 전송 | 다른 사람의 저장소가 손상 |
| 민감 정보 반영 | **공개 저장소** |
